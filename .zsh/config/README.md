# Read Me

Any `.zsh` files in this directory will be automatically sourced.

To add your own customized configuration directives, simply add new `.zsh` files to the `~/.zsh/config/` directory.

> Be sure to not edit any of the tracked files (listed below), otherwise you won't be able to use `git pull` to update.

The following is the list of files in the `~/.zsh/` directory which are tracked.

```none
.zsh/
└── config/
   ├── 00-path.zsh
   ├── 01-manpages.zsh
   ├── 02-plugins.zsh
   ├── 03-ohmyzsh.zsh
   ├── 04-alias.zsh
   └── README.md
```
